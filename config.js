const config = {
    smms: {
        // SM.MS 账号信息
        credentials: {
            username: '', // 在此填写你的 SM.MS 用户名
            password: ''  // 在此填写你的 SM.MS 密码
        }
    },
    markdown: {
        // Markdown 文件路径配置
        filePath: '',    // 在此填写需要处理的 Markdown 文件的完整路径
    }
};

module.exports = config; 